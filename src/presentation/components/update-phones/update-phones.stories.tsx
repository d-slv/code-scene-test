import React from 'react';
import {UpdatePhone} from './update-phone';

export default {
	title: 'Components/UpdatePhone',
	component: UpdatePhone,
};

const Template = args => <UpdatePhone {...args} />;

export const Default = Template.bind({});
